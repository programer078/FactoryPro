import { ArrowIconDirective } from './arrow-icon.directive';

describe('ArrowIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowIconDirective();
    expect(directive).toBeTruthy();
  });
});
